# CPP-DSA
IoT Assignment

Note: I'm aware that good practice is to have the variables be declared inside the main function due to scope, but since these are relatively simple programs, there isn't really any drawback to defining them at the top of the file, and it looks cleaner too so might as well
